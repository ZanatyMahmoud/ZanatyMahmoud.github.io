# تقرير التحقق — 20 سبتمبر 2026

الحالة الحالية: الملفات مكتملة وجاهزة للتجربة؛ النشر والاختبارات البصرية للموقع الناتج لم يكتملَا.

## الفحوص المنفذة فعليًا

- توليد `index.html` و`en.html` بنجاح من `content.json` باستخدام Python 3.
- `node --check assets/app.js`: لا أخطاء صياغة JavaScript.
- فحص HTML برمجيًا: العربية RTL والإنجليزية LTR، عنوان H1 واحد لكل صفحة، وعدم تكرار معرفات العناصر.
- جميع الروابط الداخلية تشير إلى معرفات موجودة؛ كل الملفات المحلية المشار إليها موجودة. هذا فحص ملفات، وليس فحص تحميل HTTP.
- رابط البريد في الصفحتين مضبوط على `mailto:acc.m.zanaty@gmail.com`؛ لم يُرسل بريد ولم يُختبر برنامج بريد خارجي.
- المحتوى الأساسي موجود فعليًا في HTML ولا يعتمد على حقنه بـJavaScript. اللغتان صفحتان منفصلتان، وقائمة الهاتف وتفاصيل المشروع تستخدم عناصر HTML أصلية.
- فحص نصي لكل ملفات التسليم لاستبعاد أسماء جهات العمل المحظورة والراتب ومجموعة المصطلحات المهنية غير المدعومة؛ نجح.
- مراجعة المحتوى مقابل بيانات الطلب: المهام مستقلة عن توزيع جهات العمل، أربع سنوات متزامنة مع الدراسة، المشروع أكاديمي، الدورات تدريبية، لا نسب مهارات أو نتائج مالية مخترعة.
- التباين الحسابي وفق صيغة WCAG: النص الرئيسي/الخلفية 15.81:1؛ النص الثانوي/الخلفية 9.35:1؛ النص الثانوي/السطح 8.60:1؛ الذهبي/السطح 8.92:1؛ نص الزر/الذهبي 9.62:1.
- تم فتح الموقع المرجعي في متصفح فعلي ومعاينة سطح المكتب.
- تحقق الاتصال الرسمي من أن حساب GitHub هو `ZanatyMahmoud`. الاستعلام عن المستودع المفضل أعاد 404، وكذلك واجهة GitHub العامة. لا يمكن اعتبار ذلك نفيًا قاطعًا لوجود مستودع خاص خارج صلاحيات الاتصال.

## لم تُنفّذ بعد — لا يُدّعى اجتيازها

- العرض الفعلي عند 360 و390 و768 و1440 بكسل، والتأكد من عدم التمرير الأفقي.
- فحص موقع المرجع على هاتف.
- تبديل اللغات وحفظ الاختيار عند تفعيل التخزين أو تعطيله داخل متصفح فعلي.
- فتح قائمة الهاتف وإغلاقها وEscape والتنقل بلوحة المفاتيح.
- التحقق البصري من عدم اختفاء العناوين تحت الترويسة.
- فتح زر الطباعة ومراجعة جميع صفحات المعاينة/PDF.
- تشغيل الموقع في متصفح مع JavaScript معطل.
- مراقبة Console والطلبات والأصول أثناء تشغيل الموقع.
- فحص فعلي لتفضيل تقليل الحركة ولتكبير النص.
- إنشاء مستودع أو رفع الملفات أو تفعيل GitHub Pages أو التحقق من رابط منشور.

## سبب القيد

المتصفح المتاح رفض فتح ملفات محلية بسبب سياسة عناوين المتصفح التي تسمح فقط بـHTTP/HTTPS. لم تتم محاولة تجاوز هذا القيد. اتصال GitHub المتاح لا يضم عملية إنشاء مستودع أو تفعيل Pages، والمتصفح غير مسجل الدخول. يلزم تسجيل دخول آمن لإكمال الجزء المتعلق بالنشر، ثم فحص الموقع المنشور.

## فحص ما بعد النشر

افتح رابط Pages الصادر من GitHub، ثم افحص الصفحتين والأصول والتنقل والبريد والطباعة عند المقاسات المحددة. أعد اختبار اللغة بعد إعادة التحميل، واختبر Escape وTab والروابط دون JavaScript. حدّث هذا التقرير بنتائج فعلية فقط.
